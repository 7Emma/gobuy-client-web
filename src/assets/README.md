# Dossier assets

Assets importables dans les composants/pages via `import`.

Utiliser ce dossier pour:

- illustrations
- icones locales
- images optimisees pour le bundle

Pour des assets servis tels quels (sans import), utiliser plutot `public/`.
